# Hevy Client Progress Ledger

**Purpose:** Durable handoff state for autonomous and subagent-driven development. Update this file after every implementation task and commit it with that task.

## Current state

- Design: approved and committed at `16fe62c`.
- Implementation plan: complete and committed at `06b5bc3`.
- Active task: Release-readiness requirements repair is verified on `main`; public-release configuration is in progress.
- Next task: Commit the repair, configure the canonical GitHub security gates, and publish `v0.1.0`.
- Execution mode: subagent-driven with specification-compliance and code-quality review gates after each task.

## Non-negotiable constraints

- Clean-room implementation from official Hevy and MCP contracts.
- C# 14 / .NET 10 LTS.
- Hevy API key from environment only; no persistence or telemetry.
- Stdio local default; authenticated single-tenant Streamable HTTP optional.
- Full official API coverage plus bounded deterministic composite tools.
- Red-green-refactor for production behavior.
- Small commits; run and record verification before claiming a task complete.

## Task history

| Task | Status | Commit | Verification | Notes |
|---|---|---|---|---|
| Design | Complete | `16fe62c` | Spec self-review and clean commit | Written spec approved through autonomous execution instruction. |
| Plan | Complete | `06b5bc3` | Spec coverage, placeholder, scope, and type-consistency review passed | Stable MCP SDK pinned to 1.4.1; local .NET 10 installation is Task 1. |
| Task 1 | Complete | `e6edc6a` | .NET SDK 10.0.302 installed; OpenAPI jq check returned `true`; `dotnet restore --use-lock-file` and `dotnet build --no-restore -c Release` succeeded with zero warnings | The official snapshot has no `servers` array; later release code must enforce the approved `https://api.hevyapp.com` origin itself. |
| Task 2 | Complete | `feat: model Hevy API contracts` plus review fixes | RED compile failures were observed for absent response DTO/context, request DTO/context, endpoint envelopes, constrained mutation values, forward-compatible template muscles, and the default-struct RPE bypass; focused serialization and full Release suites pass with zero warnings | Source-generated enum conversion needs explicit `JsonStringEnumMemberName` values for the snapshot's snake_case enum literals; mutation set values and RPE reject undocumented values at construction and serialization boundaries. |
| Task 3 | Complete | `feat: add authenticated Hevy read client` plus review fixes | Initial RED evidence plus review-fix RED failures for origin mutation, unsafe auth targets, environment-only options, redirect configuration, local history slicing, and inner-exception redaction; focused client tests (30) and the full Release suite (58) pass with zero warnings | Authenticated requests are checked against the exact HTTPS origin before key injection and the public production pipeline disables redirects. Exercise history is locally sliced because the official endpoint is unpaginated. Raw-key and injected-HttpClient construction are internal/test-only. |
| Task 4 | Complete | `feat: add safe Hevy mutations and retries` | RED evidence for missing mutation/retry API, direct routine response, endpoint-specific retry safety, nested validation, per-attempt request isolation, deadline-bounded Retry-After, exact-origin, date Retry-After, and single-sample jitter regressions; focused mutation (4), retry (12), and full Release solution (74) suites pass with zero warnings | Every official POST/PUT family validates before I/O and writes exact JSON. Production retries GET and only the documented full-replacement body-measurement PUT (maximum three total), creates a fresh request per attempt from operation-scoped buffered content, preserves exact-origin checks, honors deadline-bounded Retry-After with injectable collaborators, and fails ambiguous writes as `outcome_unknown`. |
| Task 4, fix round 1 | Complete | `fix: classify ambiguous Hevy writes` | RED: unselected mutation 501 returned normally and injected write transport failures became `transient_upstream`; GREEN: focused mutation (5), retry (13), and full Release solution (76) suites pass with zero warnings | Any mutation 5xx outside the selected retry set now becomes `outcome_unknown`; selected statuses still permit only read and explicitly safe body-measurement PUT retries. Both mutation send paths preserve the same classification without the production retry handler. |
| Task 4, fix round 2 | Complete | `fix: classify direct mutation failures` | RED: direct injected-client 501 responses became retryable `transient_upstream`; GREEN: focused mutation (6), retry (13), and full Release solution (77) suites pass with zero warnings | Both mutation response helpers classify every 5xx as `outcome_unknown` before the shared response mapper, without changing cancellation or 4xx client-error handling. |
| Task 5 | Complete | `feat: host MCP over stdio and authenticated HTTP` | RED: absent options types failed compilation; the empty executable produced no handshake and exited zero on invalid configuration; the HTTP stub failed all 9 initial WAF tests; review-hardening tests observed remote HTTP Origin accepted and wildcard hosts allowed. GREEN: MCP (33), transport (2), and full Release solution (112) suites pass with zero warnings | Immutable environment parsing keeps secrets out of diagnostics; production DI constructs `HevyClient` only from environment-derived `HevyClientOptions`. Stdio is protocol-only. HTTP is stateless, bearer protected with fixed-time hashed comparison, and limits Host/Origin values to explicit reverse-proxy-safe authorities. The empty DI-backed `tools/list` seam is intentional until Task 6. |
| Task 5, fix round 1 | Complete | `fix: validate MCP bearer token configuration` | RED: all 8 malformed token68 option cases passed startup unexpectedly. GREEN: focused options/HTTP (45) and full Release solution (124) suites pass with zero warnings | Startup and HTTP authentication now share the exact token68 grammar: ASCII alphanumeric plus `-._~+/`, with `=` permitted only as trailing padding. Surrounding whitespace, quoted/unicode values, invalid punctuation, and embedded padding are rejected; multiple Authorization values remain unauthorized. Fixed-time comparison and distinct-token enforcement are preserved. |
| Task 6 | Complete | `feat: expose complete Hevy MCP tool surface` | RED: empty real inventory, missing read/mutation handlers, and stale transport expectation. GREEN: MCP (65), transport (2), and full Release solution (144) suites pass with zero warnings | Exactly 22 snapshot-derived snake_case tools are exposed (14 GET tools in read-only mode). Results use authoritative structured content plus short text; inputs validate before I/O; reads propagate cancellation; writes support exact dry runs and safe guards. Since body measurements expose no `updated_at`, their replacement is conservatively force-only after current-state review. |
| Task 6, self-review fix | Complete with concern | `fix: harden MCP tool result contracts` | RED: malformed binding returned no stable structured envelope, partial pages omitted continuation metadata, and dry runs omitted validation warnings. GREEN: full Release solution 144/144, zero warnings | Binding failures now return safe correlated validation envelopes; list metadata has `truncated` and explicit continuation inputs; dry runs return `validation_warnings`. Remaining concern: output schemas advertise a typed envelope with broad `object` data/meta slots, not payload-specific per-operation schemas. |
| Task 6, fix round 1 | Complete | `fix: align MCP schemas with wire contracts` | RED: real stdio schema access failed because set type was boolean `true`; typed-output inventory and conformance failed; filtered continuations omitted required fields. GREEN: MCP 68/68, transport 3/3, full Release 148/148; zero warnings | Explicit schema normalization matches numeric RPE and official enum wires; every operation advertises typed output data/meta and actual structured content is recursively validated; event/history continuations preserve identity and filters. Body-measurement update remains force-only because upstream exposes no `updated_at`; schema, description, and results state that accepted limitation. |
| Task 7 | Complete | `feat: add agent-oriented Hevy workflows` | RED: missing cache/continuation/search/analysis types; empty composite/prompt inventories; unbounded empty-page scan; incomplete measurement-delta coverage. GREEN: cache 5/5, continuation 8/8, search 6/6, analysis 8/8, real composite/prompt contracts 5/5, MCP 101/101, transport 3/3, full Release solution 181/181; zero warnings | Two complete immutable catalogs use a process-local, size-bounded 15-minute sliding cache with coalesced loads and post-write invalidation. Five named read-only composite tools expose normalized search and deterministic UTC evidence/analysis with opaque filter-bound continuations; two MCP prompts require evidence citations and actual completed-set/end-time collection. The parent instruction's “six” composite count was clarified as a typo because the approved spec and plan name exactly five; no unnamed write operation was invented, and Task 6 guarded replacements remain authoritative. |
| Task 7, fix round 1 | Complete | `fix: harden composite pagination and cache semantics` | RED: creator/waiter cancellation interfered with a shared cache load; low-level catalogs bypassed cache; history made three upstream downloads in one invocation; page sizes 3/6/7/9 scanned 1,001–1,008 slots; 101 measurement pages threw; sparse metric evidence was omitted; evidence tokens were accepted by training summary and shifted across Monday; non-midnight weekly bounds missed an included workout; chunk-local summary fields could not reproduce a complete-period progression/gap result. GREEN: cache/low-level focused 10/10, deterministic analysis 25/25, MCP 122/122, transport 3/3, full Release solution 202/202; zero warnings | Cache fills are independent of waiter cancellation while each waiter remains cancellable. Low-level routine/template list and single reads page complete cached catalogs, honor expiry, and reload after related writes. Analysis continuations are operation/phase-specific, replay every stable input, reset an explicit 1,000-slot per-invocation scan budget, keep exact half-open UTC instants, page measurements without failure, and expose honest `complete_period`/`partial_chunk` additive metrics with timestamped progression observations and gap-completeness metadata. Exercise history makes one upstream fetch per composite invocation. |
| Task 7, fix round 2 | Complete | `fix: cancel abandoned cache fills and batch history` | RED: sole/all-waiter cancellation left the underlying catalog fill running; a 150-entry unpaginated history response produced no composite entries. GREEN: cache 9/9, deterministic analysis 27/27, client 78/78, full Release solution 207/207 across 3 test projects, Release build 6 projects; zero warnings | Cache entries track waiters atomically and cancel/evict only an incomplete fill with no remaining consumers, preserving coalescing and replacement identity. The typed client exposes the official all-history response in one request; paged reads remain local, and each composite invocation aggregates a deterministic requested window up to 1,000 with stable continuation replay. |
| Task 7, fix round 3 | Complete | `fix: bound streaming exercise history` | RED: extreme unsigned continuation pages reached client I/O before offset overflow; the unpaginated history response was fully materialized. GREEN: real streaming tests 8/8, client 86/86, MCP 129/129, full Release solution 218/218 across 3 projects, Release build 6 projects; zero warnings | Page/offset reachability rejects before I/O. The official response is parsed incrementally with existing source-generated entry metadata, one HTTP request, at most 1,000 scanned/returned entries, and an independent 16 MiB byte ceiling. Low-level and composite results expose scanned count and honest normal or terminal truncation; continuations are emitted only when the next window remains reachable. Cancellation and early response disposal are preserved. |
| Task 7, fix round 4 | Complete | `fix: validate streaming history envelopes` | RED: the streaming reader accepted missing root closure, trailing content, malformed later members, and duplicate target fields, while rejecting an unknown member before the target. GREEN: streaming 16/16, client 94/94, full Release solution 226/226 across 3 projects, Release build 6 projects; zero warnings | Bounded incremental JSON tokenization now accepts additive members before or after `exercise_history`, requires one unique array-valued target, and validates the root object plus whitespace-only EOF before reporting completeness. Unknown values are validated without materialization; only one history entry is reconstructed for source-generated deserialization at a time. Early truncation, item/byte caps, cancellation, and one-request behavior remain intact. |
| Task 8 | Complete | `feat: add privacy-safe diagnostics` | RED: diagnostics/options tests failed to compile because the safe snapshot/provider did not exist; live-gate tests failed 0/10 at the unimplemented gate; real stdio returned unknown-tool for `get_diagnostics`. GREEN: client 94/94, MCP 145/145, transport 14/14 with 2 clearly skipped live facts, full Release solution 253 passed and 2 skipped; Release build has zero warnings | `get_diagnostics` is always registered as read-only, yielding 28 normal and 20 read-only tools. Snapshots and stderr JSON records are constructed only from typed allowlisted fields. Arbitrary logger state, formatters, exception text, headers, queries, titles, timestamps, bodies, and measurements are never serialized. `None` creates no provider. Exact discovery-time live gates prevent test-body/client construction unless the required flags and nonblank existing key are present; the opted-in mutation smoke test performs a value-preserving update of an existing measurement. Fixture-pattern scans found only intentional test declarations/assertions and no production or TRX output leaks. |
| Task 8, fix round 1 | Complete | `fix: isolate diagnostic sink failures` | RED: diagnostics tests failed to compile for the absent dispatch boundary; the private measurement comparator regression failed at its deliberate placeholder. GREEN: redaction/dispatch 7/7, MCP 149/149, transport 15/15 plus 2 discovery skips, full Release solution 258 passed and 2 skipped; zero warnings | Diagnostic writes are strictly best-effort: serialization, `WriteLine`, and `Flush` failures are contained inside the provider, permanently disable that sink, and cannot change successful mutation, structured error, or cancellation semantics. The live mutation comparator checks every measurement field and fails with one fixed value-free message. Both client `validation` and MCP `validation_error` are classified as rejected validation diagnostics. |
| Task 9 | Complete | `docs: package and document hevy-client` | RED: all 3 container smokes failed because the Dockerfile was absent. GREEN: pinned multi-stage build; 3/3 image/stdio/HTTP smokes; Release build 6 projects; full solution 261 passed and 2 gated live skips; zero warnings. Four JSON configurations parse and all public documentation links return 200. | SDK and noble-chiseled runtime are pinned by official multi-architecture digests. The 56.2 MB image runs as `app`/UID 1654, has no shell/package manager or advertised port, supports a read-only root, and exposes only an exec-form .NET entrypoint. Public docs cover secret-safe configs for five major clients plus generic stdio, authenticated loopback/TLS self-hosting, operations, limitations, reporting, and clean-room contribution. No image was published. |
| Task 9, fix round 1 | Complete | `fix: harden container delivery workflow` | RED: immutable-frontend, Docker-failure policy, independent coordinator, failed-inspection cleanup, secret-backed launcher, security-release-blocker, desktop-documentation, and exact-label contracts all failed before their implementation. GREEN: 7 container/provider smokes, 13 infrastructure regressions, 278 full-solution passes plus 2 explicit live skips, and a 6-project Release build; zero warnings. Four JSON documents parse; shell syntax and diff checks are clean. | All remote Docker references are digest-pinned. CI never skips Docker failures; local skipping is limited to executable absence or narrowly recognized stopped daemons. Each smoke process owns a 128-bit-random tag, resolves an immutable ID, and cleans only that tag even after failed identity inspection. macOS/Linux use tested system-secret-store launchers; Windows uses a secure-prompt, process-only GUI launcher. Public release remains blocked until a canonical remote and GitHub private vulnerability reporting are configured and verified. Exact SemVer, 40-character revision, and HTTPS source labels are inspected. No image was published. |
| Task 9, fix round 2 | Complete | `fix: restrict Docker skips to local endpoints` | RED: five remote endpoint cases, including the reported `tcp://build-host:2375` refusal, were incorrectly skipped. GREEN: availability 18/18, infrastructure 22/22, full Release solution 287 passed plus 2 explicit live skips, and a 6-project Release build; zero warnings. | Daemon-absence messages are endpoint-parsed. Only absolute local Unix sockets, local Windows Docker named pipes, and exact localhost/IP-loopback TCP/HTTP(S) endpoints can skip outside CI. Remote hostnames, IPv4/IPv6, SSH, permission, TLS, context, and compatibility failures fail; CI continues to fail every unsuccessful probe. |
| Task 9, fix round 3 | Complete | `fix: bind Docker absence diagnostics` | RED: the initial 9-case adversarial suite produced 7 failures and 2 already-safe invalid forms; explicit configured-host/context tests failed compilation; 2 self-review quote-pairing regressions then failed. GREEN: availability 42/42, infrastructure 46/46, full Release solution 311 passed plus 2 explicit live skips, and a 6-project Release build; zero warnings. | One anchored single-line diagnostic binds one exact endpoint to one recognized absence reason. Windows quotes must balance and encoded-request/opened pipe names must match an exact known local Docker pipe. No endpoint repair, cross-clause refusal matching, userinfo, suffix, path/query/fragment, or invalid port is accepted. Explicit remote `DOCKER_HOST` or non-local `DOCKER_CONTEXT` disables local skipping. CI always fails absence; a missing executable still skips locally. |
| Task 9, fix round 4 | Complete | `fix: resolve effective Docker endpoints` | RED: 4 absent-async-seam compile failures; 13 URI-canonicalization/raw-grammar failures; 6 accepted control-bearing diagnostics; 1 propagated resolution timeout; 6 unsafe context-name runner invocations. GREEN: Docker/infrastructure 95/95, real container/provider 7/7, full Release solution 360 passed plus 2 explicit live skips, and a 6-project Release build; zero warnings. | Failed probes now honor `DOCKER_HOST` or inspect the actual named/persisted context endpoint without daemon access; unresolved state fails closed. A bounded raw parser accepts only exact local Unix/npipe or explicit-port loopback network grammars, with no paths, dot segments, encoding, userinfo, query, or fragment. C0/C1 controls and Unicode line/paragraph separators are rejected before regex/parsing. CI and missing-executable behavior remain unchanged. |
| Task 9, fix round 5 | Implemented; re-review pending | `fix: harden Docker context resolution` | RED: 26/123 infrastructure cases failed across reversed context/host precedence, unsafe configured/persisted names, and unprotected positional arguments; a follow-up whitespace-only context also failed. GREEN: Docker/infrastructure 125/125, real container/provider 7/7, full Release solution 390 passed plus 2 explicit live skips, and a 6-project Release build; zero warnings. | Nonempty `DOCKER_CONTEXT` now takes precedence over `DOCKER_HOST`; only when it is absent may the host or persisted active context be used. Both configured and persisted names must match Docker's ASCII context-name grammar within 256 characters. Inspection retains its trusted format and places `--` before the validated positional name. Docker 29.5.2 accepted the protected form and inspected `desktop-linux` to the expected local named pipe. |
| Task 9, compatibility correction | Implemented; re-review pending | `fix: match pinned Docker endpoint precedence` | RED: both actual-runtime cross-variable cases failed because the policy inspected `DOCKER_CONTEXT`; blank-host fallthrough cases remained green. GREEN: focused precedence 4/4, Docker/infrastructure 127/127, real container/provider 7/7, full Release solution 392 passed plus 2 explicit live skips, and a 6-project Release build; zero warnings. | Pinned Docker CLI v29.5.2 source/runtime resolve nonempty `DOCKER_HOST` before `DOCKER_CONTEXT`, contrary to the current CLI environment-variable documentation. The policy now matches that compatibility behavior, then falls through to configured or persisted context only for an absent host. Strict ASCII context grammar and protected `--` inspection remain unchanged; an in-code primary-source note requires re-audit on Docker client bumps. |
| Task 10 | Complete | `ci: automate verified releases` | RED: delivery contracts exposed missing workflows, validators, action/tool locks, executable scripts, repository-wide credential scanning, digest-only release ordering, and explicit indentation policy. GREEN: delivery 9/9; full Release suite 401 passed plus 2 gated live skips; locked restore and 6-project Release build completed with zero warnings; real container smoke 7/7; actionlint, OpenAPI validation, repository audit, shell syntax, and diff checks are clean. | CI is read-only and secret-free. Release stages an untagged multi-platform digest, verifies metadata/SBOM/provenance/signatures, and promotes a serialized SemVer convenience tag only as its final step; GHCR does not enforce tag immutability, so consumers pin the digest. The local image is `sha256:67bf5ebd9a077484fef1db40ffd9db144d51df7dbe55b46f725c464e904ff4fe` (56,217,198 bytes); a registry-free OCI build proved real amd64/arm64 output. Public release remains fail-closed until the canonical repository and private advisory gate are verified. Nothing was pushed, tagged, or published. |
| Task 10, fix round 1 | Complete | `fix: harden verified release transaction` | RED: 6/28 initial focused controls passed; authenticated Registry v2, promotion, complete SPDX, audit adversaries, exact pins/ranges, Cosign SHA, and reproducibility controls failed. GREEN: delivery/security 31/31; full Release suite 423 passed plus 2 gated live skips; locked restore, format, and 6-project Release build have zero warnings; real container smoke 7/7. Two no-cache exact-toolchain OCI exports matched index `sha256:d1eb7294b90983c895caaf3ddc114ec81e510e3aa2ef86e22eb7931ab9745bb7`, amd64 `sha256:3a14bf1700c41a63ac6152f435374aa80e1ece2e982d01bc2926fdbb5dabb44f`, and arm64 `sha256:41358f9b34fbfae98be8fe7301b974af734102086e78999c413911b660442719`. | GHCR lookup performs the complete Bearer challenge/token/HEAD flow without credential arguments or output. Final promotion is absent/same/conflict aware and remains the last fallible command under repository concurrency plus a protected exclusive-writer environment. Invocation-specific inline attestations are excluded from the reproducible staged index; checksum-pinned Syft emits validated SPDX 2.3 and pinned GitHub actions attach provenance/SBOM afterward. CI and release execute the two-export reproducibility gate before authentication. Nothing was pushed, tagged, or published. |
| Task 10, fix round 2 | Complete | `fix: bind staged release to reproducible build` | RED: focused delivery/security ran 50 tests with 30 passing and 20 failing across executable repro/installer behavior, exact descriptor count, digest binding, Buildx binary pinning, and dotted/tilde credential scanning. GREEN: focused 50/50; full Release suite 442 passed plus 2 gated live skips; locked restore, format, and 6-project Release build have zero warnings; real container smoke 7/7; actionlint, OpenAPI, repository audit, JSON, shell syntax, and diff checks are clean. Two no-cache exports under Buildx 0.35.0 commit `a319e5b15052cf6557ceb666eb8ff6e32380b782` and BuildKit 0.31.2 matched index `sha256:1fbb68b863c9409dc4e1066b8548746adba5e137ff147e28a92f440d8e781fdc`, amd64 `sha256:eff405e8f41bd2306dcee26d77412806ff10d06bc23ced3f8fecd833751f9350`, and arm64 `sha256:8b7af4f68f4adbb5cf400cb43df37406100c41f9bb55c0b90c485c050d39c7f0`. | The checksum-verified Buildx plugin is installed under isolated `DOCKER_CONFIG` before the pinned setup action. Auditing setup-buildx-action v4.2.0 showed that any nonempty `version` input forces its release downloader, so the workflow omits that input, lets the action detect the verified plugin, and proves the active version/commit immediately afterward. Reproducibility outputs are written only after two exact exports match. Before evidence or signing, the raw staged bytes, build-action digest, reproducibility index digest, and both exact platform digests must all agree; any third, unknown, or inline descriptor fails closed. Nothing was pushed, tagged, or published. |
| Task 10, fix round 3 | Complete | `fix: reject ambiguous release inputs` | RED: focused delivery/security ran 64 tests with 55 passing and 9 failing because OCI validators accepted concatenated JSON roots, multiline digests, and fractional sizes, reproducibility failure mutated existing output, and the actionlint downloader was not exercised through executable seams. GREEN: focused 68/68; full Release suite 460 passed plus 2 gated live skips; locked restore, format, and 6-project Release build have zero warnings; real container smoke 7/7; actionlint, OpenAPI, repository audit, JSON, shell syntax, and diff checks are clean. Two no-cache exports under Buildx 0.35.0 commit `a319e5b15052cf6557ceb666eb8ff6e32380b782` and BuildKit 0.31.2 matched index `sha256:e5a03d9ce8257bd31e1834f00551c4220046b57c5bb1586b35420c98ab3dbf11`, amd64 `sha256:08798692e8a7a8b10e81b9a423583e23273be7028b2dd38380e972ef930a1f0c`, and arm64 `sha256:2a3ee5641f854459dee8df8fef9c0720f378f824f546464d138df29b206a5826`. | OCI layout/index validation now requires one complete duplicate-key-free JSON root, lexical integer schema/size tokens, exact descriptor counts/platforms, and strict single-line SHA-256 digests. Reproducibility output is appended once only after both exports and all digests validate and match. The actionlint runner is executable-tested for its exact locked download, hardened curl arguments, checksum-before-execution ordering, cleanup, and exit propagation. Independent re-review found no remaining issues. Nothing was pushed, tagged, or published. |
| Task 10, fix round 4 | Implemented; re-review pending | `fix: bound OCI index processing` | RED: focused boundary/release contracts ran 27 tests with 18 passing and 9 failing because registry output was captured without a byte ceiling, both local OCI parsers loaded unbounded documents, and descriptor-size tokens above signed 64-bit maximum were accepted. GREEN: focused 27/27; delivery/security 81/81; full Release suite 473 passed plus 2 gated live skips; 6-project Release build and format have zero warnings; real container smoke 7/7; actionlint, OpenAPI, repository audit, shell syntax, and diff checks are clean. Two no-cache exports under checksum-verified Buildx 0.35.0 matched index `sha256:126fb29b90e20ee6d04c07200c8ec7fcdc986304294ae116bd93fdc48ccb8875`, amd64 `sha256:185c239ebb45ec55e5b8bbf3e3602dac90d57cfae9adabc92986c23d3e2b4c28`, and arm64 `sha256:988110b9bc383657790f8422ccd373ec93be84ce430744da4175170351a2961b`. | Registry index capture now streams through an executable fixed-limit helper, reads at most byte 4,194,305, preserves producer/pipeline failure, cleans partial data, and atomically replaces its destination only through byte 4,194,304 without touching `GITHUB_OUTPUT`. Both local parser paths stat and bounded-read before JSON decoding. Every raw descriptor size must be a lexical positive integer no greater than `9223372036854775807`; the boundary is accepted and the next token is rejected in validator and reproducibility paths without floating-point coercion. Nothing was pushed, tagged, or published. |
| Final whole-branch fix wave | Complete | `fix: close final safety review` | RED: real and injected timeout tests exposed uncaught internal cancellations; three committed-write/readback cases returned retryable failures; ordinary responses accepted missing contracts, oversized bytes/pages, inconsistent pages, and an overflowing catalog page count; exercise-template page size 100, history integer fidelity/schema, repeated per-workout templates, internal dispatcher faults, and adversarial capture destinations/statuses each failed focused regressions before implementation. GREEN: Client 107/107, MCP 163/163, Transport 233 passed plus 2 explicit live skips (503 total passes); bounded capture 7/7; container smoke 7/7; locked restore, format, 6-project Release build, actionlint, OpenAPI, repository audit, shell syntax, and diff checks are clean. Two no-cache exports under checksum-verified Buildx 0.35.0 matched index `sha256:24116dafc9012123b49bd14c835dacc77232917f7e7fa229ce8f2f06ff5a442e`, amd64 `sha256:904b52a8aa61123f9e0a44c3e7d4b9a4def0956d1764dd08bfd1aab04e05b08b`, and arm64 `sha256:34633f271fcb67a62a577826d4231de83b2be983e4e93e77d57726721f7c97ee`. | Caller cancellation still propagates; read timeouts are safe/retryable while mutation timeouts are non-retryable `outcome_unknown`. Successful writes with failed readback return non-retryable `committed_readback_failed`, direct agents to fetch state without replay, and invalidate the affected template cache. Ordinary JSON is capped at 4 MiB with required contract/page validation; history preserves official integer fields; progression aggregates once per template/workout. The capture helper rejects directory targets and preserves the actual reader failure over consequential SIGPIPE. CI/release strict-loader/toolchain duplication and mutation-validator duplication were intentionally retained: consolidating them in this final safety wave would expand pinned supply-chain and schema-binding scope without a current divergence, while executable contracts cover each boundary. Nothing was pushed, tagged, or published. |
| Post-final-review response-safety repair | Complete | `fix: harden committed mutation responses` | RED: focused Client response/mutation tests ran 48 with 31 failures and 17 passes: all malformed, missing-required, and oversized direct 2xx mutation bodies surfaced as `unexpected_response`; nonempty zero-count pages and downstream null fields were accepted; collection-null coverage exposed nested gaps. A subsequent discriminator regression proved the serializer leaked `NotSupportedException` for a missing event type. GREEN: focused Client 60/60 and MCP mutation/cache/read 37/37; full Client 157/157, MCP 169/169, and Transport 233 passed plus 2 explicit live skips (559 total passes); locked restore, format, 6-project Release `-warnaserror` build, actionlint, OpenAPI, repository audit, JSON/shell syntax, and diff checks are clean. The rebuilt production image is `sha256:d7c6aec8d47e74d5200b184f7192fe6a776773680dbcd698bbbc36f5544c0a0d` (56,225,618 bytes) and all 7 container smokes pass. | A confirmed 2xx mutation is now the commit boundary: later body size, decode, contract, timeout, or other failures are non-replayable `committed_readback_failed`, while genuine caller cancellation still propagates. Routine/template caches are conservatively invalidated immediately before writes, so no post-commit exception or cancellation can retain stale state. Explicit model-graph validation rejects null elements in every top-level/nested response collection and requires fields consumed by tools, search, and analysis while retaining additive unknown-field tolerance; invalid polymorphic envelopes also normalize to stable `unexpected_response`. Remote and cached paging both accept only the canonical empty page 1/count 0 and reject requests beyond a computed page count. The exact reproducibility gate was not rerun because Dockerfile/build/release inputs were unchanged; the preceding pinned two-export gate remains the supply-chain result and the new compiled source was exercised through the rebuilt image. Nothing was pushed, tagged, or published. |
| Post-final-review page-invariant repair | Complete | `fix: enforce canonical empty pages` | RED: Client response-contract tests ran 52 with 40 passes and 12 expected failures because every ordinary paged family accepted empty collections with positive counts on both first and later pages; the focused cache regression separately failed because an empty positive-count catalog was cached. GREEN: response contracts 52/52 and cache 12/12; full Client 180/180, MCP 170/170, and Transport 233 passed plus 2 explicit live skips (583 total passes); locked restore, format, 6-project Release `-warnaserror` build, actionlint, OpenAPI, repository audit, JSON/shell syntax, and diff checks are clean; container smoke 7/7. | Remote and cached page validation now share the exact canonical invariant: an empty collection is valid if and only if `page=1` and `page_count=0`. Empty positive-count pages are rejected at any position, nonempty pages remain valid, and additive response fields remain forward-compatible across workouts, workout events, routines, exercise templates, routine folders, and body measurements. Docker/release inputs were unchanged, so the two-export reproducibility gate was not rerun. Nothing was pushed, tagged, or published. |
| Release-readiness requirements repair | Complete | `fix: close release readiness gaps` plus CI portability fix | RED evidence covered leaked client-side environment ownership, catalog search above 1,000 items, incomplete OpenAPI binding, missing diagnostic identifiers, ambiguous upstream request IDs, a trailing-comment audit bypass, and a credential scan that required PCRE2. GREEN: Client 182/182, MCP 173/173, and Transport 235 passed plus 2 explicit live skips (590 total passes); locked restore, format, 6-project Release `-warnaserror` build, actionlint, OpenAPI, repository audit, and diff checks are clean; real container smoke 7/7. Two no-cache exports under checksum-verified Buildx 0.35.0 matched index `sha256:8cece74db80de3e2232c420628a70e4349f161127d3bf2bb0ba669fba8f7acde`, amd64 `sha256:794de3df303884962b67d29f5beedcf9094fdcae1cdf9120741aff6078e56624`, and arm64 `sha256:e037433150be8b079856f5c5384c5428d0665209696fee0cc0f9511157af1eaa`. | Environment ownership stays in `Hevy.Mcp`; catalog search resumes across bounded page windows; every pinned component and successful endpoint request/response recursively binds to handwritten wire types; diagnostics retain only allowlisted operation and safe upstream request identifiers, including direct outcome-unknown mutations. Exercise history fails closed with an explicit terminal reason because its unpaginated upstream response cannot support safe deep continuation under the same per-call bound. Tests use FluentAssertions 7.2.2, and executable audits reject `Assert.*` plus C# line comments outside strings while preserving essential XML documentation. The credential scan now uses ripgrep's portable regex engine. Independent specification review found no release blocker; standards review findings were fixed and regression-tested. |

## Resume instructions

1. Read `docs/superpowers/specs/2026-07-26-hevy-client-design.md`.
2. Read `docs/superpowers/plans/2026-07-26-hevy-client-implementation.md`.
3. Read this ledger and `git log --oneline --decorate -10`.
4. Run `git status --short --branch`; preserve unrelated user changes.
5. Resume the first incomplete plan task using test-first development.
6. Do not trust prior success claims without rerunning the task verification.
